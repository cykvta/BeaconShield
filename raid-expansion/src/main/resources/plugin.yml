name: BeaconShield-RaidExpansion
version: '${project.version}'
main: icu.cykuta.beaconshieldraid.BeaconShieldRaidExpansion
api-version: '1.20'
depend:
  - BeaconShield

# /bsraid is registered through the API command framework (CommandMap), not
# here, so it is not declared under 'commands'. Its permission nodes
# (beaconshield.raid.start/join/admin) are declared by the sub-commands
# themselves and registered on startup, so they are not listed here either.
